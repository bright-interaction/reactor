# Substrings exempted from the mirror pre-flight secret scan
# (scripts/mirror-secret-preflight.sh). One per line. A history line containing
# any of these is not treated as a credential.
#
# Only add a value that is provably synthetic, and say why. Anything real must be
# purged from history instead, never allowlisted.

# HTTP client test: asserts a per-request Bearer overrides the client default, so
# the default is set to a value that says it should be overridden.
should-be-overridden

# Secret-rotation test: the "after rotation" value, padded to length with x's.
newsecretvaluexxxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook HMAC tests. Both spellings appear across the history of the same test.
hmac-secret-shhh
shared-hmac-shhh

# Login handler test, the xkcd 936 passphrase used as a readable sample password.
YOUR_PASSWORD

# oauth SSRF test (internal/oauth/ssrf_test.go). The token endpoint is made to
# return this string with a 403, and the test asserts the exchange error does NOT
# echo it: it is the canary for the read primitive the fix closes, so it has to
# LOOK like something worth exfiltrating. AKIAIOSFODNN7EXAMPLE is AWS's own
# published documentation placeholder, not a key. Allowlisted as the full fixture
# string rather than the AKIA prefix, so a genuine AWS key still trips the gate.
AKIAIOSFODNN7EXAMPLE-internal-metadata-payload
