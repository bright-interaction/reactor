# {{.Slug}}

Webhook trigger that resolves a vault secret then calls an external API.
Generated by `reactor new webhook-step-secret {{.Slug}}` on {{.Date}}.

Before running:

```bash
reactor vault add --name external-api-key --service my-api --provider shared-secret --value <your-key>
reactor vault grant {{.Slug}} external-api-key
```

Then build + register + serve as usual.
