# {{.Slug}}

Cron-triggered workflow (default: hourly). Generated by
`reactor new scheduled-rotation {{.Slug}}` on {{.Date}}.

Edit `Trigger.Schedule` in main.go + `triggers[0].schedule` in dag.json
to change the cadence. Standard cron syntax; `CRON_TZ=...` prefix sets
timezone.
